extends layout 

block content 
    h1= title 

    h2= genre 

    p Books in this genre:

    dl
        each book in genre_books 
            dt
                a(href=book.url) #{book.title}
            dd 
                p #{book.summary}
        else 
            p This genre has no books 